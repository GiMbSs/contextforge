# ContextForge

**Less Context. Better Intelligence.**

ContextForge is a context engineering engine. This repository is currently in the
implementation-foundation stage.

## Development preview

```bash
python -m contextforge --help
python -m contextforge --version
```

No scanning, indexing, provider or patch behavior is implemented in increment I001.
